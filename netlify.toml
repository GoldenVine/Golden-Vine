[build]
  command = "pnpm install --frozen-lockfile && pnpm --filter @workspace/golden-vine run build"
  publish = "artifacts/golden-vine/dist/public"

[build.environment]
  PORT = "3000"
  BASE_PATH = "/"
  CI = "true"

[functions]
  directory = "netlify/functions"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
